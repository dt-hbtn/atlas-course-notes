# Intro to arrays, pointers, and strings in C

## Compile
Simply run the `make` command inside this code directory and it will compile the demo application.

## Run
Usage of the demo application looks as follows:
```bash
./reverso "string you want to reverse"
```
